# Documentation Placeholders

The following files are placeholders and should be replaced with actual assets:

- `architecture.png` – system architecture diagram
- `workflow.png` – end-to-end workflow diagram
- `database-schema.png` – MongoDB schema/ERD representation
- `demo.gif` – short product demo clip
- `screenshots/` – UI screenshots for key user flows
